export interface TimerProps {
    sec: number,
    color: string,
    duration: number,
    isPause: boolean,
    isStart: boolean,
}

export interface TimerLineAngBlockProps {
    duration: number,
    isPause: boolean,
}