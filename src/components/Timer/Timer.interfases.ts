export interface TimerProps {
    sec: number,
    color: string,
    duration: number,
    isPause: boolean,
}

export interface TimerLineAngBlockProps {
    duration: number,
    isPause: boolean,
}